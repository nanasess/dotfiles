;;----------------------- window settings ---------------------------
;; (if (boundp 'window-system)
;;     (setq initial-frame-alist
;; 	  (append (list
;; 		   '(height . 60)
;; 		   '(width  . 82))
;; 		  initial-frame-alist)))
;; (setq default-frame-alist initial-frame-alist)
(provide 'x-init)
